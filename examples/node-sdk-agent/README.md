# Node SDK agent example

```bash
cd examples/node-sdk-agent
export BLEKLINE_WORKSPACE_TOKEN="blw_..."
export BLEKLINE_API_URL="https://app.blekline.com"
node index.mjs
```

Uses `@blekline/client` with `BLEKLINE_CLIENT_SURFACE=sdk`.
