# OpenAI Codex

1. Copy [`config.toml.example`](config.toml.example) → `config.toml`.
2. Set `BLEKLINE_WORKSPACE_TOKEN` and `BLEKLINE_API_URL` in the env blocks.
3. Restart Codex — invoke `blekline_mask_prompt` on a test prompt with a fake secret.

`BLEKLINE_CLIENT_SURFACE=codex`

Docs: [Codex MCP](https://app.blekline.com/docs/mcp/codex)
