# Plugin assets

Use `webapp/public/branding/blekline_webclip.png` as the listing logo.

```bash
cp webapp/public/branding/blekline_webclip.png plugins/cursor/assets/logo.png
```

`logo.png` in this folder is that webclip copy (or add it with the command above before marketplace upload).
