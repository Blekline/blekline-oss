---
name: init-hooks
description: Initialize Blekline Codex hooks in this workspace.
---

```bash
npx @blekline/codex-hooks init
```

Remind the user to trust hooks and that silent auto-send uses ingress on the OpenAI Responses API. Token placeholder: `blw_replace_with_workspace_token`.
